#ifndef _IO_H_
#define _IO_H_

#include <vector>
#include <string>

#define VERBOSE

void set_color(int color);
void show(std::string str);
void warning(std::string str);
void bye(std::string str);
double ECOclock();

#endif
